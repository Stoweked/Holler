# Default dashboard route

Despite the historical folder name, `page.tsx` renders the transactions dashboard
at `/`. `layout.tsx` reuses the dashboard layout. No sign-in is required.
Marketing components remain under `src/features/marketing/` for reference.
